# Development progress

## 2026-09-22

- Created `FairFedDrift_integration_plan.txt` with small implementation/check steps after reading AGENTS.md, current strategy hooks and upstream revision `51dec5e`. Source and document readback checks completed; integration and runtime validation remain pending.

Keep one brief dated entry per development step: change, validation, and any remaining limitation. Record completed work only; implementation plans remain in their separate files.

## 2026-09-16

- Updated `AGENTS.md` to match this repository: excluded FedCollab, marked Ditto as planned, and corrected inherited assumptions. Read back and verified.
- Created `FRAMEWORK_IMPROVEMENTS_INTEGRATION_PLAN.md` covering seven framework improvements. Read back and verified; implementation tracked separately from planning.
- Implemented PGF export alongside PNG/PDF, directory creation, optional strict errors, and figure cleanup. Line/bar exports and XeLaTeX compilation visually verified; failure handling checked with Matplotlib 3.4.3. Full simulation not run.
- Created this progress log and added the requirement to update it after every development step to `AGENTS.md`. Read back and verified.
- Implemented shared auto/cpu/cuda selection, consistent model/strategy placement, guarded CUDA setup, and equivalent full-precision training. Four regression checks and a synthetic two-client/two-round CPU simulation passed on PyTorch 2.8.0+cpu; CUDA hardware execution unverified. Updated device docs and Phase B status.
- Implemented reusable L2 model-distance history for primary and FedRC models, collected post-aggregation/pre-distribution and persisted as whole-model/layer logs. Four focused checks and a two-client/two-round integration run passed; future FedEx optimization can consume `model_distance_history` directly.
- Implemented staged local/global evaluation, separate structured logs, label-swap class metrics, and optional sample-weighted server reporting. Eight focused checks and a two-client/two-round CPU run passed; client_log remains post-training local metrics.
- Attempted the integrated CPU validation step. The focused device, distance, and evaluation checks passed (12 tests), but the end-to-end partial-participation run is blocked because Phase C was explicitly skipped and the simulation loop still assigns all clients each round; no integrated result is claimed.
- Ran the remaining integrated validation with full participation. The existing two-client/two-round CPU run produced evaluation, class-metric, legacy client/server, drift, and model-distance logs; all 12 focused regression checks passed. CUDA execution and larger strategy/dataset combinations remain unverified.
- Revised `Ditto_integration_plan.txt` after studying original Ditto commit `26d9b29` and FedTNT commit `692a0e1`. The plan now defines dual-model lifecycle, Ditto-only weighted aggregation, fixed-lambda-first delivery, explicit personalized logs, deterministic dynamic-lambda splitting, drift behavior, and focused acceptance checks; implementation remains pending.
- Implemented Ditto Phase 1: strategy registration, server selection, sample-count-weighted upload aggregation, input validation, stable non-floating-buffer handling, and early rejection of unsupported deep hierarchies. Five Ditto checks and all 17 focused CPU tests passed; personalized training remains pending.
- Implemented Ditto Phase 2: validated configuration, post-warm-up personal-model initialization, immutable global references, ordinary upload training, persistent fixed-lambda proximal training, and run-wide drift behavior. Eight Ditto tests and all 20 focused CPU tests passed; personalized evaluation/logging and dynamic lambda remain pending.
- Implemented Ditto Phase 3: explicit non-mutating personalized evaluation, model-role metadata, post-training history, state/client/class structured logs, and train/eval-mode restoration. Nine focused Ditto tests, 12 existing tests, and a two-client/one-round fixed-lambda CPU simulation passed; dynamic lambda remains pending.
- Implemented Ditto Phase 4: persistent seeded validation splits, isolated per-batch candidate updates with optimizer-state continuity, deterministic tie breaking, and detailed selected-lambda logs. Fixed and dynamic two-client CPU simulations and the complete 25-test suite passed; CUDA and deep Ditto hierarchies remain unverified.
- Ran real-MNIST integration validation on CPU: fixed Ditto, dynamic Ditto, and FedAvg each completed three-client/two-round full-participation runs with a label-swap transition, evaluation, class, and distance logs. Final upload accuracies were 0.1233, 0.1467, and 0.1133 respectively; the tiny subset makes these execution checks rather than benchmarks.
- Cross-checked Ditto against FedTNT commit `692a0e1` and appended the remaining `main.py` entry-point integration steps to `Ditto_integration_plan.txt`. Algorithmic differences that intentionally preserve this framework's training semantics are documented separately from the missing runnable Ditto configuration.
- Implemented Ditto entry-point steps 1-4: centralized `main.py` Ditto settings and added a disabled-by-default MNIST Ditto experiment with full participation, a flat server, and dedicated output paths. Compilation and source/diff checks passed; focused tests could not import because the available Python environments lack project dependencies. Executing this exact block and its FedAvg compatibility run remain pending.
- Added disabled-by-default Ditto handles under all five `main.py` dataset sections: MNIST, Fashion-MNIST, CIFAR-10, CIFAR-100, and Tiny ImageNet-200. All share the centralized Ditto settings and use dataset-specific output paths; extracted-block parsing, coverage checks, `main.py` compilation, and diff checks passed.

- 2026-09-22: Simplified the FairFedDrift plan around existing files/functions and explicit main.py settings, constructor/run handles and dataset coverage. Verified function references and document readback; implementation remains pending.

- 2026-09-22: Completed FairFedDrift step 1.1: added RecoveryAlgorithm.FAIRFEDDRIFT and marked the plan step complete. Python import/value checks passed for the new constant and all eight existing strategy names; strategy implementation and wiring remain pending.

- 2026-09-22: Completed FairFedDrift step 1.2: added the strategy class, factory, package exports and explicit NotImplementedError aggregation placeholder; updated the plan. Syntax and isolated factory/failure checks passed, and full package import/factory checks passed on Python 3.11. Default Python hits existing union-annotation incompatibility; algorithm implementation and server wiring remain pending.

- 2026-09-22: Implemented FairFedDrift step 1.3 in server_fn(), reusing existing model creation and Server initialization; updated the plan. Isolated actual factory/initializer checks passed for MNIST on CPU for FairFedDrift, FedAvg, Oracle, FedEx and Ditto. Full server import is blocked by missing SciPy on Python 3.11; aggregation and remaining flow wiring are pending.

- 2026-09-22: Implemented FairFedDrift step 1.4: exported parameter resolution and setup validation following Ditto's pattern, with explicit group thresholds and CPU/full-participation/flat-layout checks. All six focused configuration tests passed on Python 3.11; updated and read back the plan. Automatic invocation during experiment construction remains pending flow wiring; full server import still requires SciPy.

- 2026-09-22: Set FairFedDrift's default history window to 100 communication rounds, clarified round-based retention and pending eviction in the plan, and updated configuration checks. All seven focused tests passed on Python 3.11; actual history storage/eviction remains unimplemented.

- 2026-09-22: Implemented FairFedDrift step 2.1 with set_dataset_groups()/get_dataset_groups() in data/utils.py, preserving dataset types and two-field training batches. All 11 focused data/configuration tests passed, including nested subsets, IID/non-IID partitioning, shuffled group loaders and invalid metadata; plan read back. Real group generation and experiment attachment remain pending.

- 2026-09-22: Revised the FairFedDrift plan for PaLA's client-group drift: single local loss/threshold, optional sensitive metadata, scalar merging, 100-round history, PaLA metrics and main.py handles. Reopened configuration step 1.4 and preserved completed-work history; read back the revised plan. This is documentation only; current dual-threshold code still needs migration.

- 2026-09-22: Completed revised FairFedDrift step 1.4: replaced dual thresholds with required fairfeddrift_loss_threshold, added obsolete-key migration errors, and updated tests and plan. All 13 focused configuration/data checks passed on Python 3.11; defaults and setup checks remain unchanged. Validator invocation during experiment construction and algorithm implementation remain pending.

- 2026-09-22: Removed unused sensitive-group metadata helpers from data/utils.py and their dedicated test_fairfeddrift_data.py; updated the plan to reflect cleanup. All nine remaining FairFedDrift tests and the existing two-field dataset/loader smoke check passed on Python 3.11; source search found no remaining helper/group_ids references. Multi-client fixture and algorithm implementation remain pending.

- 2026-09-22: Completed test-only FairFedDrift step 2.2 with a six-client before/after fixture and separate ground-truth identities. All 13 focused tests passed on Python 3.11, including four fixture checks for labels/counts, two-field batches, isolation and repeatability. Updated/read back the plan to preserve main.py-controlled final drift configurations; detector and empty-data validation remain pending.

- 2026-09-22: Completed FairFedDrift step 2.3 drift-path review and guarded start/end handlers against resetting the strategy to FedAvg. Added a real drift-function test for configurable swaps/timing, stationary clients, loader refresh and no Oracle reassignment; main.py and drift transformations unchanged. All 14 FairFedDrift tests passed on Python 3.10; broader discovery passed 26 tests with two Ditto import errors from missing ucimlrepo. Plan read back; full simulation and future detector isolation remain pending.

- 2026-09-22: Implemented step 2.4 helpers: selected-sample CPU DatasetSnapshot and per-client ClientDataHistory with 100-round expiry, duplicate-arrival protection and expired-round reporting. All 19 FairFedDrift tests passed on Python 3.10, including five history checks; plan read back. Snapshots freeze transformed inputs. Runtime wiring, cross-arrival deduplication and cluster-history cleanup remain pending; existing functions/main.py were not changed.

- 2026-09-23: Completed step 2.5 helper prepare_fairfeddrift_decision_loader(), reusing DatasetSnapshot and the existing loader utility to capture current training samples once without test-data access or history mutation. All 22 FairFedDrift tests passed on Python 3.10; plan read back. Candidate loss evaluation and runtime invocation remain pending; existing sampling/training functions, main.py and the simulation loop are unchanged.

- 2026-09-23: Completed step 3.1 with evaluate_fairfeddrift_loss(): sample-mean cross-entropy on prepared decision data, no gradients, restored module modes, and empty/nonfinite-loss rejection. All 27 FairFedDrift tests passed on Python 3.10, including five new loss checks; updated/read back the plan. Existing evaluation functions and simulation loop unchanged; selection and runtime wiring remain pending.

- 2026-09-23: Completed step 3.2 selection with per-client previous-loss references, inclusive absolute threshold, active-order ties and a new-cluster signal when all candidates fail. All 32 FairFedDrift tests passed on Python 3.10, including five selection checks; plan updated/read back. Actual cluster creation is step 3.3; runtime wiring remains pending. Existing framework functions and main loop unchanged.

- 2026-09-23: Completed step 3.3 cluster initialization/assignment, independent copies of the first active model on rejection, increasing IDs and copy-failure rollback. All 37 FairFedDrift tests passed on Python 3.10; plan updated/read back. Existing framework functions/main loop unchanged; merge history and shared runtime/server wiring remain pending.

- 2026-09-23: Completed step 3.4 historical scalar-loss merge distances and pure complete-linkage updates, checking the pinned upstream source. Reused retained snapshots, loaders and loss evaluation; current-timestep/expired data excluded and infinity replaces the upstream rejection sentinel. All 42 FairFedDrift tests passed on Python 3.10; plan updated/read back. Merge execution and runtime history wiring remain pending; existing framework functions/main loop unchanged.

- 2026-09-23: Completed step 3.5 cluster merging, retained assignment recording/relabeling, sample-weighted copies via set_parameters(), complete linkage and atomic merge-pass commit. All 48 FairFedDrift tests passed on Python 3.10 after correcting a test-fixture input dtype; plan updated/read back. Existing framework functions unchanged. Upload aggregation and runtime arrival/history wiring remain pending; integer buffers follow Ditto's largest-contributor convention per merge.

- 2026-09-23: Audited steps through 3.5 against supplied paper pages, pinned Git code and PaLA boundaries; saved/read back FairFedDrift_fidelity_review.txt and updated plan. All 48 tests plus 200 selection/100 merge formula checks passed. Reproduced unnecessary evaluation of history-free models; added pending correction 3.4a. No production changes; runtime scheduling, deduplication and full historical training remain unverified/pending.

- 2026-09-23: Revisited step 3.4 and implemented audit correction 3.4a: merge calculations now skip models without retained history and skip all inference with fewer than two history-bearing clusters, while preserving counts and infinite ineligible distances. Added raising-model regression coverage. All 50 FairFedDrift tests passed on Python 3.10; updated/read back the integration plan and fidelity review. Runtime scheduling and history wiring remain pending.

- 2026-09-23: Completed step 3.6 FairFedDrift upload aggregation using validated client sample counts and the existing weighted-state helper/set_parameters(). All 55 FairFedDrift tests passed on Python 3.10, including five aggregation checks; plan updated/read back. FedAvg and Ditto are unchanged. Server upload/count routing and runtime integration remain pending step 4.5.

- 2026-09-23: Completed step 4.1 by tracing constructor setup, warm-up, aggregation/distribution, drift refresh and local training through existing code. Updated/read back the plan with boundaries for pre-data validation, shared cluster-model ownership, batch candidate decisions and tagged uploads; no code behavior changed. Inspection only; no full network import/simulation because ucimlrepo is unavailable. Round-0 model ownership remains for 4.2/4.5.

- 2026-09-23: Completed step 4.2 with `link_clients_to_fairfeddrift_servers()` to keep learned cluster IDs distinct from flat server positions and rebuild client/server links after reassignment. Four focused Python 3.10 tests and `git diff --check` passed; plan updated/read back. Shared strategy state and round-0 model initialization remain pending step 4.3.

- 2026-09-23: Completed step 4.3a with `initialize_fairfeddrift_runtime()` called before the first FairFedDrift client-training pass. It shares the strategy object and binds cluster 0 to the server's post-warm-up model without resetting later state. Four focused Python 3.10 tests passed; plan updated/read back. Frozen-candidate loss decisions remain step 4.3b; no simulation-loop edits.

- 2026-09-23: Completed step 4.3b with frozen-candidate whole-client decision scoring before `train_client_models()`' per-client loop, using one sampled-data snapshot loader per client and routing learned IDs through flat server slots. Connected FairFedDrift parameter resolution/setup validation before dataset loading and passed the scalar threshold into the strategy. All 65 FairFedDrift tests and four device tests passed on Python 3.10; `git diff --check` passed; plan updated/read back. End-to-end client upload routing and aggregation remain pending 4.4-4.5; simulation loop unchanged.

- 2026-09-23: Completed step 4.4 after tracing learned server positions through `train_client_models()` into `Client.fit()`. Fixed the active-drift dispatch so FairFedDrift uses the existing ordinary `models.utils.train()` path; added checks for assigned-model download and fit before/during/after drift. All 67 FairFedDrift tests passed on Python 3.10; plan updated/read back. Upload grouping and aggregation remain pending 4.5; simulation loop unchanged.

- 2026-09-23: Completed step 4.5 with training-cluster tags on client uploads, a FairFedDrift aggregation branch in the existing server dispatch, and sample-count forwarding through `Server.train()`. Untagged warm-up uploads are restricted to cluster 0; normal uploads are validated and grouped by their training tag, not current server position. Counts follow Ditto's `len(local_trainset)` convention. All 71 FairFedDrift tests passed on Python 3.10; plan updated/read back. History-aware retraining/merge scheduling and end-to-end simulation checks remain pending; simulation loop unchanged.
- 2026-09-23: Completed FairFedDrift step 4.6 phase switching: the configured base method is active outside drift and the recovery method during drift; FairFedDrift's strategy object is retained when returning to it. Oracle base servers are parked/restored when FairFedDrift is the drift recovery; Oracle recovery over FairFedDrift base and FedRC pairings are rejected due incompatible topology. Updated/read back the integration plan. All 73 FairFedDrift tests, four device tests and `git diff --check` passed; full simulations/history execution remain unverified.
- 2026-09-23: Completed step 5.1 with per-client, per-cluster retained-history loaders and selected-sample counts. ClientDataHistory now shares identical captured input/label versions across arrival references and releases each version on its final expiry; historical assignment rounds stay distinct. Focused history tests and full FairFedDrift tests passed on Python 3.10; plan updated/read back and `git diff --check` passed. These helpers are not connected to local historical training yet; step 5.2 remains.
- 2026-09-23: Completed step 5.2 with `Client.fit_fairfeddrift_history()`: deep-copy each matching server cluster model, train it through existing `models.utils.train()`, and retain detached uploads with exact sample counts. `client.model` and server models remain untouched; each train call creates its own optimizer. All 78 FairFedDrift tests and four device tests passed on Python 3.10; `git diff --check` passed and plan was read back. Aggregation/orchestration wiring remains for steps 5.3-5.4.
- 2026-09-23: Completed step 5.3 by extending FairFedDrift server dispatch to route per-client history uploads by active cluster and combine them with current-assignment uploads using each source's sample counts. Server and strategy validate matching IDs/counts and model state; history-only cluster aggregation is supported, stale cluster IDs fail, and empty client history clears prior uploads. All 85 FairFedDrift tests passed on Python 3.10; active runtime scheduling and expiry cleanup remain step 5.4.
- 2026-09-23: Completed step 5.4 scheduling via `prepare_fairfeddrift_timestep()` in the existing client-training orchestration: one decision per configured communication-round bucket (independent of drift ground truth), prior-history merge before decisions, one captured arrival per timestep, per-round history advancement/expiry cleanup, and refreshed historical uploads for sampled clients. Updated/read back the plan. All 88 FairFedDrift tests and four device tests passed on Python 3.10; `git diff --check` passed. No full simulation run; final communication-round scheduling intentionally retains the framework's ordinary aggregation/evaluation behavior without upstream's separate test-only timestep. `network.py` unchanged.
- 2026-09-23: Completed step 6.1 in `server_hierarchy_evaluate()`: when FairFedDrift is the active server strategy, evaluate every active cluster server using the existing server-test-data or client-derived metrics paths and weighting; preserve empty-server placeholders and ignore cached inactive strategy objects. Added three focused checks. All 91 FairFedDrift tests and `git diff --check` passed on Python 3.10. The result structure and simulation loop are unchanged; full network simulation remains unverified.
- 2026-09-23: Completed step 6.2 audit and minimal traceability update: staged evaluation still resolves each client through the current flat server slot and remains non-mutating; its records now add the learned FairFedDrift cluster ID. Distance records likewise include that cluster ID while retaining `ModelDistanceHistory` timing after aggregation/before distribution, `model_id='primary'`, and true whole-model L2. Thirteen evaluation/diagnostic tests and all 92 FairFedDrift tests passed on Python 3.10; `git diff --check` passed. No simulation-loop changes; full simulation remains unverified.
- 2026-09-23: Completed step 6.3 structured FairFedDrift logging. The strategy retains scalar per-round events for decision candidate losses/sample counts, assignments/new clusters, merges, active clusters and historical-upload counts; `build_fairfeddrift_state_log()` adds resolved settings and final assignment state without snapshots or model tensors. Added `constants.Logs.FAIRFEDDRIFT_STATE_LOG` and save the structured log after the run through `write_structured_log()`, leaving `client_log.pkl`, `evaluation_log.pkl` and the simulation loop unchanged. Five focused logging/scheduling checks, four staged-evaluation checks, all 94 FairFedDrift tests and `git diff --check` passed on Python 3.10. Full simulation remains unverified.
- 2026-09-23: Verified FairFedDrift `client_log.pkl` uses the same per-round/per-client `(loss, accuracy)` records as Oracle/FedEx, and `server_log.pkl` uses Oracle's nested flat-server records; FairFedDrift's server count is variable across rounds. Its strategy-state log is supplementary. Confirmed no `.ipynb` files exist in this checkout and revised step 6.4 to leave PaLA metric calculations to the separate notebooks. Twelve focused output/evaluation tests and `git diff --check` passed; no PaLA metric functions added.
- 2026-09-23: Completed main.py step 7.1 by adding shared `fairfeddrift_parameters` beside the Ditto settings: loss threshold 0.1 (explicitly provisional), 100-round history, one communication round per timestep and seed 42. Extracted the main.py dictionary and confirmed `resolve_fairfeddrift_parameters()` accepts it; 23 focused strategy/runtime tests and `git diff --check` passed. No experiment handles or runs were added; threshold calibration remains pending.
- 2026-09-23: Completed main.py step 7.2 with `fairfeddrift_recovery_parameters`: both phase methods select FairFedDrift and shared 7.1 settings are merged in. The first draft included an unnecessary `cluster_count`; step 7.3 removed it along with the unused FedRC count after confirming the flat handle supplies its initial server and FairFedDrift learns the active count. Added AST-based config checks without importing main.py or creating a network; 24 focused tests passed at 7.2, then 25 after the 7.3 cleanup and handle addition.
- 2026-09-23: Completed main.py step 7.3 with a disabled MNIST FairFedDrift network constructor and run call, matching the adjacent 50-round/full-participation flat-layout experiment. Removed unused FedRC and Oracle cluster-count keys from its dedicated recovery config; retained only generic alpha values consumed by network setup. Added checks that the entire handle remains commented and FairFedDrift configuration has no fixed cluster-count settings. All 25 focused config/strategy/runtime tests and `git diff --check` passed; dedicated output paths are step 7.4.
- 2026-09-23: Completed main.py step 7.4 by adding dedicated FairFedDrift MNIST plot/log paths to the disabled run call. The config test checks both paths are distinct and remain inside the commented handle; two main-config tests and `git diff --check` passed. No experiment ran.
- 2026-09-23: Completed main.py step 7.5 by adding disabled FairFedDrift constructor/run blocks and dedicated paths for Fashion-MNIST, CIFAR-10, CIFAR-100 and Tiny ImageNet-200, matching neighboring dataset round counts. Expanded source-level config checks to verify all five dataset handles are commented, flat, full-participation and use the shared FairFedDrift settings/output paths. Three tests and `git diff --check` passed; no experiment ran and dataset support is not implied.
- 2026-09-23: Completed the step 7.6 activation audit. All five FairFedDrift handles remain commented out. Found 15 executable network constructors and three active run calls in main.py (single-value CIFAR-10 FedEx, Oracle and FedAvg); constructors can load datasets without running simulations. Left existing experiment selection unchanged and documented that a FairFedDrift-only smoke run must wait until main.py is isolated. No simulation run performed.
- 2026-09-23: Completed step 8.1 by auditing the existing focused FairFedDrift tests across their dedicated modules; whole-client loss, no sensitive metadata, threshold/tie/reference rules, empty data, weighted aggregation, merge logic and historical assignments were already covered. Avoided duplicating them in test_fairfeddrift_strategy.py. All 98 `test_fairfeddrift*.py` tests passed on Python 3.10; end-to-end simulation remains pending.
- 2026-09-23: Completed step 8.2 with tests/test_fairfeddrift_simulation.py, running the real FederatedNetwork loop on synthetic MNIST-shaped CPU data. It verifies no-drift baseline assignment, a deterministic learned split, retained-history merge, 3-round eviction, 2-round timestep cadence, evaluation and strategy/client/server logs. Stubbed only the unavailable, unused `ucimlrepo` fetch import. All 99 FairFedDrift tests passed on Python 3.10; real-data main.py smoke remains pending isolation of its existing active experiments.
- 2026-09-24: Completed step 8.3a with an isolated CPU MNIST smoke using the existing main.py drift/FairFedDrift configuration, ten clients, five rounds, one local epoch and cached-data subsets (5,000 train/1,000 test). Five strategy events and five evaluation records were validated. Temporary main.py routing was removed, so normal handles/experiment selection are unchanged; outputs were disposable. PNG/PDF plots completed; PGF warned on missing MiKTeX underscore.sty. A bootstrap stubbed the unused, unavailable ucimlrepo import. Full PaLA scenario comparisons remain pending step 8.3b.
- 2026-09-24: Ran the step 8.3b scenario/method pilot in an isolated temporary main.py route: A/B/C × FedAvg/FedEx/Oracle/FairFedDrift, 15 rounds, ten clients, one local epoch, seed 42, first 1,000 cached MNIST training and 250 held-out test examples. Scenario C reached all three states at rounds 6/10/11; A/B reached two at 6/10. All 12 runs saved standard client/server/evaluation logs; each client log had 15 rounds and each staged evaluation log had 15 records. FairFedDrift produced 15 state events per scenario with its 100-round retention setting. Outputs are in logs/pala_fairfeddrift_scenario_pilot_15r/ for the separate notebooks; no evaluation metrics were calculated here. An initial 50-round CPU attempt was stopped during the FairFedDrift case after several minutes; partial outputs were removed. The temporary route is removed and normal main.py experiment selection is unchanged. This pilot does not satisfy the full-data 50/100-round comparison or threshold calibration; those and the measured history-cost/eviction checks remain pending step 8.3b.
- 2026-09-24: Completed step 8.4: 26 tests passed across test_device.py, test_model_distance_diagnostics.py, test_evaluation.py, test_ditto_strategy.py and test_ditto_simulation.py. The unused ucimlrepo import was stubbed only for the two Ditto modules because it is unavailable in Python 3.10; no dependency was installed. The device tests emitted two existing AMP deprecation warnings. Reused the small real-MNIST pilot runs to verify three FedAvg cases kept one server evaluation and three Oracle cases kept all three flat cluster-server evaluations; the existing uniform-FedAvg invariant test passed. The 50/100-round comparison, threshold calibration/history-cost checks (step 8.3b) and README/AGENTS documentation updates (step 8.5) remain open.
- 2026-09-24: Enabled FairFedDrift setup on either configured CPU or CUDA after auditing candidate loss, historical loss/merge evaluation, client history training, aggregation and snapshot handling. The CPU-only check was an unnecessary guard; tensor computation uses get_device(), while CPU snapshot hashing is intentional. Added CUDA-device acceptance checks without requiring local GPU allocation. The full FairFedDrift CPU suite and real CUDA runtime remain to be verified; the GPU server is the target for the next smoke run.
- 2026-09-24: Verified the CUDA-guard change with all 99 focused FairFedDrift tests on Python 3.10, including flat/full-participation setup checks for both torch.device('cpu') and torch.device('cuda'); the CUDA case validates configuration only and allocates no CUDA tensor. CPU end-to-end simulation passed. This host has PyTorch 2.8.0+cpu, so actual CUDA training remains unverified and must be smoke-tested on the remote GPU before full experiments.
- 2026-09-24: Duplicated FairFedDrift_integration_plan.txt as FairFedDrift_integration_plan.md for the remote IDE agent; verified both copies match byte-for-byte.

- 2026-09-24: Centralized experiment seed selection in `main.py` and propagated it through required simulation settings, per-network construction/run reseeding, Ditto/FairFedDrift settings and rotation-drift sampling. Added focused checks for repeatable Python/NumPy/PyTorch streams, IID partitions and model initialization. Source compilation passed; runtime tests are unverified because the exposed Python 3.10/3.12 environments lack PyTorch. `git diff --check` is obscured by pre-existing `main.py` CRLF working-tree changes.

- 2026-09-24: Changed the `main.py` experiment-device default from `auto` to explicit `cuda`, preventing silent CPU fallback and retaining the existing clear error when CUDA is unavailable. Source compilation and readback passed; actual CUDA execution remains unverified on this CPU-only host.

- 2026-09-24: Added dataset/method completion messages immediately after all 43 `main.py` simulation calls, including disabled blocks, FedEx layer cases and the alpha sweep; messages remain commented whenever their matching run is commented. Coverage count, source compilation and readback passed; no simulation was run.

- 2026-09-24: Updated MNIST/CIFAR-10 fairness notebooks and checkpoint copies to count AEQ horizons with an exclusive upper endpoint, generate both paper horizons (5 and 11/20 rounds), retain two-round smoothing, and derive captions/labels from the chosen horizon. Corrected the reversed d1/d0 calculation and cleared stale outputs. Numerical verification pending; external experiment logs have not been rerun.
- 2026-09-24: Verified all four edited fairness notebook files compile and pass synthetic numerical checks for exact 1/5/11/20-round horizons, two-round smoothing, end-of-training truncation, onset-only mode, all six ratio/horizon tables, captions and d1/d0 orientation. Checkpoints match main notebooks. Actual experiment tables remain unregenerated because external logs were not executed.
- 2026-09-24: Made PNG/PDF/PGF export explicit in all four drift plotting notebooks and their checkpoint copies. Routed standalone legends through configure_and_save_plot with their existing tight crop/padding preserved. Shared exporter unchanged; validation pending.
- 2026-09-24: All eight plotting notebook/checkpoint files passed code compilation and export-call checks. Executed each legend helper against the actual repository exporter with savefig intercepted: PNG/PDF/PGF requests, tight crop, padding, rc restoration and figure cleanup passed. Actual LaTeX rendering and external experiment-log regeneration were not run.
- 2026-09-24: Added saved_logs_fairfeddrift/client_log.pkl to four performance notebooks alongside the existing Oracle path, purple FairFedDrift curves and standalone legends, and a FairFedDrift column to both fairness notebooks for all ratios/horizons and LaTeX outputs. Updated checkpoints and cleared stale outputs. Existing PGF exports/horizon definitions preserved; synthetic validation pending.
- 2026-09-24: Synthetic validation passed for all six notebooks and six checkpoints: four-method log reads, FairFedDrift statistics, purple curves in 8 plots per standard notebook/14 for CIFAR-10 focus variants, legends, and six four-method fairness tables with independently checked horizon means and LaTeX headers. Save calls intercepted to avoid generating experiment artifacts; external FairFedDrift data and actual PGF rendering remain unverified.

